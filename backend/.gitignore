# dependencies
node_modules/
.pnpm-store/

# build
dist/
*.tsbuildinfo

# env & secrets — NEVER commit real secrets
.env
.env.*
!.env.example

# prisma generated client
src/generated/
prisma/*.db

# test — Jest coverage output (anchored to repo root so it does not match src/coverage/)
/coverage/

# misc
.DS_Store
*.log
